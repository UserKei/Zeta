# TODO

## 候选事项：答辩演示脚本

当前不执行，仅作为答辩阶段候选准备项。

如果后续确认需要答辩，再准备一条完整演示路径：

1. 模型配置
2. 知识库
3. Markdown 上传
4. 分段管理
5. 检索测试
6. Agent 问答
7. 对话日志标注入库

配套准备内容：

- 准备 2-3 份企业场景 Markdown 文档。
- 准备固定问题集，覆盖检索命中、引用追溯、标注入库。
- 检查页面跳转、空态、错误提示和明显 UI 问题。

## 候选事项：图片 / OCR / 扫描 PDF 多模态入口

当前不执行，仅作为下一阶段多模态知识生产候选项。

如果后续需要继续推进多模态，可以先按下面路线设计：

1. 扫描 PDF 和图片先走 OCR，提取文本后进入现有 Chunk 流程。
2. 图片类材料可以用视觉模型生成图片描述，再作为可检索分段入库。
3. 原始图片、扫描 PDF 继续保存为源文件，回答引用仍回溯到 Document / Chunk。
4. 真正的图文多模态向量检索后续单独评估，不和当前文本检索链路混在一起。

## 候选事项：Chat Citation 引用口径调整

Chat Citation 已默认只保存回答引用的前 3 个命中分段，避免知识热度被所有检索候选摊平。
后续候选：把引用数量、相似度阈值或 Rerank 阈值做成 Agent / KnowledgeBase 可配置项。
默认建议先保留固定上限，等演示数据和真实使用反馈稳定后再增加配置面板。
